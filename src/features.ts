/** Feature switches for work that is built but not ready to ship. */

/** Multi-device sync (SyncPage). Disabled for now: greyed out in the sidebar and command palette. */
export const SYNC_ENABLED = false;
