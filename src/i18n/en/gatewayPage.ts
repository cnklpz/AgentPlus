import type zh from "../zh/gatewayPage";

const en: typeof zh = {
  // time / breaker
  minSec: "{m}m {s}s",
  sec: "{n}s",
  breakerOpen: "Since {at} · retrying automatically in {wait}",
  breakerHalfOpen: "Pause expired; the next request will test whether it has recovered",
  upstreamError: "Upstream error",
  // flashes
  resumedOne: "Route resumed; the next request will be sent normally",
  resumedAll: "All routes resumed",
  gatewayOn: "Local gateway started",
  gatewayOff: "Local gateway stopped",
  forwardAddedReplaced: "Route added; switching the original provider URLs to {base} was added to pending changes",
  forwardAdded: "Route added: {base}",
  // header / hero
  title: "Local gateway",
  intro: "Forwards provider requests on this machine: clients can call it with Chat Completions, Responses or Anthropic Messages, and the gateway converts to the protocol the provider supports (including streaming and tool calls).",
  online: "Gateway online",
  notRunning: "Gateway not running",
  off: "Gateway off",
  copyUrl: "Copy URL",
  heroRequests: "{n} request|{n} requests",
  heroFailures: "{n} failure|{n} failures",
  heroActive: "{n} in progress",
  idleHint: "Once started, it listens only on 127.0.0.1 and can't be reached from other machines or web pages. Each agent calls it with its own gateway key (filled in when written); the gateway then uses the keys from the provider library upstream.",
  port: "Port",
  portInvalid: "Port must be between 1024 and 65535",
  switchPort: "Switch to new port",
  stop: "Stop gateway",
  start: "Start gateway",
  pausedBanner: "{n} route paused after repeated errors (circuit breaker)|{n} routes paused after repeated errors (circuit breaker)",
  resumeAll: "Resume all",
  staleKeysBanner: "{n} agent provider still uses an old gateway key|{n} agent providers still use an old gateway key",
  staleKeysHint: "The gateway now gives every agent its own key, to check requests and count traffic per agent. The old key still works for now, but calls made with it can't be told apart.",
  updateKeys: "Update keys",
  resumeNow: "Resume now",
  step1: "Start the gateway",
  step2: "\"Add route\": pick a provider or enter a custom URL",
  step3: "\"Connect agents\": once applied, traffic goes through the gateway",
  // routes
  routes: "Routes",
  addRoute: "Add route",
  noRoutes: "No routes yet",
  noRoutesHint: "Add a route: pick an existing provider or enter a custom URL. Afterwards you can also choose \"Via local gateway\" when editing a provider in an agent.",
  upstreamMissing: "The upstream was removed from the provider library; this route is unavailable",
  tripped: "Tripped: {reason}",
  weightTitle: "The unified endpoint splits traffic by this weight",
  weightChip: "Weight {w}",
  chipBroken: "Broken",
  chipPaused: "Paused",
  chipTripped: "Tripped {wait}",
  chipProbe: "Probing next",
  chipActive: "Active",
  copyGatewayUrl: "Copy gateway URL",
  deleteRoute: "Delete route",
  // log
  requestLog: "Request log",
  noRequests: "No requests yet.",
  passthroughSuffix: " · passthrough",
  streamSuffix: " · stream",
  // unified entry
  unified: "Unified endpoint",
  modelCount: "{n} model|{n} models",
  routeCount: "{n} route|{n} routes",
  copyUnified: "Copy unified endpoint",
  unifiedHint: "Agents only need this one URL: the gateway picks a route by the requested model, splits by weight when several routes serve the same model, and fails over automatically. Turn on \"Use local gateway\" when adding a provider in an agent to use it; there you can also select just some of the routes (the URL becomes {example}).",
  unifiedExample: "/routeA+routeB/v1",
  splitTitle: "{name}: weight {weight} ({pct}%)",
  moreModels: "{n} more model…|{n} more models…",
  unknownModels: "Model lists not known yet for {names}: the gateway asks the upstream on the first request, or you can add common models under \"Providers\".",
  // add dialog
  chooseProvider: "Choose a provider",
  pickExisting: "Existing provider",
  customUrl: "Custom URL",
  pickHint: "The URL and API key come from this provider; it's added to the provider library first if it isn't there yet.",
  replaceTitle: "Replace existing provider URLs",
  replaceNone: "No agent uses this URL directly right now.",
  agentProvider: "{agent} \"{provider}\"",
  replaceOn: "{list} will be switched to the gateway route (added to pending changes, takes effect once applied); deleting the route can restore them.",
  replaceOff: "When on, {n} agent provider using this URL will be switched to the gateway route.|When on, {n} agent providers using this URL will be switched to the gateway route.",
  namePlaceholder: "e.g. My relay, Chat group",
  upstreamUrl: "Upstream base URL",
  urlInvalid: "Must start with http:// or https://",
  upstreamProto: "Upstream protocol",
  protoHint: "The protocol the upstream actually supports; the gateway converts requests made in other protocols.",
  keyHint: "Stored in the local provider library (~/.agentplus) and used only by the gateway when forwarding.",
  autoStart: "The gateway starts automatically after adding.",
  adding: "Adding…",
  // route body
  breaker: "Circuit breaker",
  breakerCounting: "Consecutive errors so far: {fails} (the route pauses at {threshold}): {reason}",
  pausedReason: "{paused}: {reason}",
  paused: "Paused",
  breakerWhenTrips: "{when} · tripped {n} times in a row, the pause doubles each time",
  pausedDetail: "{when}. While paused, requests through this route get a 503 with the reason above, and the unified endpoint skips it; \"Test\" below still works, and a passing test resumes the route.",
  connectAgents: "Connect agents",
  linked: "{name} · connected",
  addToAgentTitle: "Add a provider in {agent} that points to the gateway ({api})",
  connectHint: "Added to pending changes; takes effect once applied. You can also switch an existing provider to the gateway in the agent's provider details.",
  otherClients: "Other clients",
  copyFullUrl: "Copy full URL",
  passthrough: "Passthrough",
  converted: "Converted",
  modelId: "Model ID",
  testModel: "Model to test",
  testTitle: "Send a very short request through the gateway using the selected protocol",
  startFirst: "Start the gateway first",
  weight: "Load-balancing weight",
  weightSaved: "Weight saved",
  weightHint: "For requests to the unified endpoint, when several routes serve the same model, they're split by weight; if one fails (unreachable, 5xx, 429), the next one is tried automatically.",
  hideAdvanced: "Hide advanced settings",
  showAdvanced: "Advanced: model mapping, pause, delete",
  modelMap: "Model mapping",
  modelMapHint: "One per line: requested model=model sent upstream; * matches all",
  modelMapSaved: "Model mapping saved",
  saveMap: "Save mapping",
  routePaused: "Route paused",
  routeResumed: "Route resumed",
  pauseRoute: "Pause route",
  resumeRoute: "Resume route",
  stopHint: "Once stopped, gateway URLs set in agents stop working; remember to switch them back to direct in those agents.",
  // breaker settings
  breakerToggle: "Pause a route after repeated errors",
  breakerDesc: "Errors include: upstream unreachable, 5xx, 429 (rate limited), 401/403 (invalid API key), 402 (insufficient balance) or an interrupted stream. While paused, requests get a 503 naming the error and the unified endpoint skips the route; when the pause ends, one request is let through as a probe: success resumes the route, failure doubles the pause (up to 10 minutes).",
  breakerOff: "Circuit breaker off",
  breakerOn: "Circuit breaker on",
  breakerForm: "After {threshold} consecutive errors, pause for {cooldown} seconds",
  thresholdLabel: "Consecutive errors",
  cooldownLabel: "Pause (seconds)",
  breakerSaved: "Circuit breaker settings saved",
};

export default en;
